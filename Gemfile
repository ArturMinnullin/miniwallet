# frozen_string_literal: true

ruby '3.0.1'

source 'https://rubygems.org'

gem 'bitcoin-ruby', git: 'https://github.com/lian/bitcoin-ruby', require: 'bitcoin'
gem 'warning' # to supress warnings from bitcoin-ruby

group :development do
  gem 'rspec'
  gem 'pry'
  gem 'webmock'
end
